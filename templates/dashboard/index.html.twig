{# templates/dashboard/index.html.twig #}
<ul>
    {% for product in products %}
        <li>
            <a href="{{ path('product_view', {'id': product.id}) }}">{{ product.name }}</a> - {{ product.price }}
        </li>
    {% endfor %}
</ul>
